- [x] As skills tem o nome `stack-{stackname}`, e no seu conteúdo referenciam arquivos apontando para o caminho `stack-{stackname}-{skillname}`. Porém isso gera um problema porque essas skills não são para uso aqui no projeto, mas são templates para serem usados após uma inicialização via script de instalação, que ao final viram `{stackname}-{skillname}`. Ou as referências nos arquivos de skills devem considerar o nome conforme estiver após instalação, ou ajustar o script para também ajustar o conteúdo.
- [x] Considear opção onde não deixar essas skills de stack dentro do diretório de skills do próprio `.claude` para não confundir entre skills de desenvolvimento deste projeto, e as skills de desenvolvimento nos projetos que usam o framework após inicialização via script.
- [x] Unificar skills e SOLUTIONs das stacks `dotnet` e `dotnetblazor`. O objetivo seria uma skill dotnet que considera a implementação de vários tipos de componentes .NET, e que devem seguir os templates TheCleanArch. Então as instruções devem considerar criar os componentes de acordo com os tipos que os templates fornecem. Workers, WebApis, WebApps, testes, testes de api, testes de apps usanto `bunit` por exemplo, etc.
