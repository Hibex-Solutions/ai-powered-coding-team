<!--
  Template — Especificação do PROBLEMA e das REGRAS DE NEGÓCIO do seu projeto.

  Este documento descreve o problema que o seu software se propõe a resolver e as
  regras de negócio que regem sua operação. NÃO descreve como o problema é resolvido
  — a solução é responsabilidade de `SOLUTION.md`.

  Responsável: analista de negócio.
  Pré-requisito para: `SOLUTION.md` (arquiteto) e implementação (engenheiro).

  Preencha este arquivo ANTES de qualquer implementação de funcionalidade. Cada regra
  de negócio deve ter um identificador estável e ser testável — `business-reviewer`
  valida que toda regra tem implementação e teste correspondentes.

  Este arquivo é incluído no contexto do assistente de IA via diretiva @-include.
  Não use cabeçalhos H1 (#) nem H2 (##) — as seções devem iniciar em H3 (###).
-->

